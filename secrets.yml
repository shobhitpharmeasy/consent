# secrets.yaml
# This file contains sensitive information that should remain outside version control.
# Keep this file secure and add it to your .gitignore file if using Git.

CONSENT_DB_PASSWORD:       "52"
# Optional: Define the DB username if you wish to override the default specified in application.yaml.
CONSENT_DB_USERNAME:       "shobhit.aggarwal"

MAIL_PASSWORD:             "yxuq zcew gohc krbw"
MAIL_USERNAME:             "vjindu@gmail.com"

CONSENT_SECURITY_PASSWORD: "consent"
